# Legacy Go2-W Description

This package is legacy and is superseded by `hexlab_go2w_description` for current ROS, RViz, and Jetson visualization.

It is not built by default because this directory contains `COLCON_IGNORE`.

It may remain temporarily as a reference for older URDF and mesh values while the HEXLab description is adopted.
